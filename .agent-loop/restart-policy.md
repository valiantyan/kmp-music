# 长跑 Agent 重启策略

## 当前状态

- 状态: idle
- 活跃批次: 无

## 默认策略

- 空闲状态下不自动重启、不自动派发任务、不自动运行验证。
- 新批次开始时，分发会话必须重新建立契约、读取进度、读取日志、读取评分表和读取本文件。
- 如果未来批次中断，应从当时的 `progress.md`、`log.md` 和对应 issue 文件恢复，而不是从本 idle 文件推断已完成工作。


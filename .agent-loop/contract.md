# 长跑 Agent Harness 契约

## 当前状态

- 状态: idle
- 活跃契约: 无

## 默认说明

当前没有活跃的长跑任务或顺序批次。

下一次启动长跑 Agent Harness 时，先根据用户目标重新建立任务契约、队列、门禁和终止条件，再开始执行、验证、评分和记录。

已完成批次的历史证据只保存在 `.agent-loop/archive/` 下；活跃契约不携带旧批次目标、队列、提交哈希或归档清单。

## 契约变更规则

只有当用户改变目标、接受新的验收标准，或要求修改运行框架契约时，才修改本文件。
